{
  "name": "rspack-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch 'src' --ext ts --exec 'rspack build --mode development && node dist | pino-pretty'",
    "build": "rspack build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rspack/cli": "0.7.5",
    "nodemon": "2.0.22",
    "pino-pretty": "10.3.1"
  },
  "dependencies": {
    "fastify": "^4.14.1",
    "graphql": "^16.6.0",
    "graphql-yoga": "^3.7.2"
  }
}
