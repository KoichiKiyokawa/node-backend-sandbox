{
  "name": "project",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ./apps/server run start:dev",
    "build:packages": "pnpm --filter './packages/**' run build",
    "build:app": "pnpm build:packages && pnpm --filter ./apps/server run build"
  }
}
